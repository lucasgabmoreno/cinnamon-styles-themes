# Cinnamon Styles Themes
Simplify the complete installation of a theme with all its variations on the Cinnamon desktop. You can install it as root with `sudo` or leave it in the home directory.

## Install
```
sudo curl -L https://raw.githubusercontent.com/lucasgabmoreno/cinnamon-styles-themes/refs/heads/main/cinnamon-styles-themes -o /usr/local/bin/cinnamon-styles-themes && sudo chmod +x /usr/local/bin/cinnamon-styles-themes
```

### Mint defaults all
```
cinnamon-styles-themes mint-defaults-all
```
Preview - Source
